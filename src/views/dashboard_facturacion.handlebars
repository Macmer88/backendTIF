{{!-- Este es el archivo dashboard_facturacion.handlebars --}}
<div class="dashboard-header">
    <h1>Dashboard: {{tituloPagina}}</h1>
</div>

<hr>

<div class="widget">
    <h2>Facturación por Mes</h2>
    <p>Datos generados por el Procedimiento Almacenado `SP_Reporte_Facturacion_Por_Mes`.</p>

    <div class="report-buttons">
        <a href="/api/ver1/reportes/facturacion?formato=pdf" class="report-button pdf" target="_blank">Descargar PDF</a>
    </div>

    <table class="stats-table">
        <thead>
            <tr>
                <th>Año</th>
                <th>Mes</th>
                <th>Total Facturado</th>
            </tr>
        </thead>
        <tbody>
            {{#each facturacion}}
                <tr>
                    <td>{{this.Anio}}</td>
                    <td>{{this.Mes}}</td>
                    <td>${{this.Total_Facturado}}</td>
                </tr>
            {{else}}
                <tr>
                    <td colspan="3">No hay datos de facturación para mostrar.</td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</div>