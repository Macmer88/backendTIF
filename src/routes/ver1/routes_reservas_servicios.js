/*

CÓDIGO EN CONSTRUCCIÓN - NO UTILIZAR


import { Router } from 'express';
import * as controller from '../../controllers/ver1/controller_reservas_servicios.js';

const router = Router();

router.get('/', controller.mostrarReservasServicios);
router.get('/:id', controller.mostrarReservaServicioPorId);
router.get('/reserva/:reserva_id', controller.mostrarPorReserva);
router.post('/', controller.crearReservaServicio);
router.put('/:id', controller.updateReservaServicio);
router.delete('/:id', controller.borrarReservaServicio);

export default router;

*/